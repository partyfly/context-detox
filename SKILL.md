---
name: context-detox
description: 按上下文工程理论对刚完成的工作做一次"上下文清毒":以七件套为审计面、四种失败模式为病理、四标准为验收、四步法为处方,列举本次被记忆/技能/摘要/检索材料污染的地方,产出清理清单并引导执行,防止污染沉淀劣化后续工作。当用户说"清毒""清理上下文""context detox",或一段大任务收尾时使用。
---

# 上下文清毒(context-detox)

**对象**:不是本次对话,而是任务中读写过、且**会被下次装进窗口的持久物**——记忆文件、技能/规则文档、材料库、摘要交接、工程注释。窗口本身会被清空;持久物里的毒会跨会话遗传。

**理论骨架**(全片映射,一层不缺):七件套=审计面(污染住在哪)/四失败模式=病理(长什么样)/四步法=处方(怎么清)/四标准=验收(什么算干净)。

## 工序(四步,不可跳过)

1. **机械预扫**:`python3 scripts/scan.py --memory <记忆目录> --skills <技能目录>`
   产出机器可查的嫌疑清单(分层追加、无日期裁定、断链、触发面过宽、重名)。机器先跑,人后judge——嫌疑≠有罪。
2. **人工审计**:读 `references/procedure.md`,按"七件套 × 四病理"查表逐面过一遍本次任务触过的持久物,填**工件一:审计表**。
3. **开清理单**:按 procedure.md 的模板填**工件二:清理清单**(问题→病理定性→处方动作→改哪个文件)。有破坏性的动作(删除、改技能正文)先给用户确认;纯追加范围注记、标注待验证可直接执行。
4. **执行与验收**:逐条执行后,对每件改过的持久物过**四标准四问**(相关/结构/时机/压缩),四问皆过才关单;汇报"改了什么/标了什么待验证/留了什么没动及原因"。

## 两条硬规矩

- **清毒自身不许中毒**:删除或改写任何断言前,先重读它指向的原文;拿不准的标"待验证+日期",不许顺手删。
- **修源头,不立注意事项**:发现冲突,改败方所在的源文件并写明优先级链;禁止只在清理单里留一句"下次注意"。

工件模板、查表、边界(什么不许清)见 `references/procedure.md`。
