---
name: context-detox
description: 按上下文工程理论对刚完成的工作做一次"上下文清毒":以七件套为审计面、四种失败模式为病理、四标准为验收、四步法为处方,列举本次被记忆/技能/摘要/检索材料污染的地方并引导清理,防止污染沉淀劣化后续工作。当用户说"清毒""清理上下文""context detox",或一段大任务收尾时使用。
---

# 上下文清毒(context-detox)

理论底座:上下文=模型每一步能看见的全部;窗口有限且塞多必腐(context rot)。清毒的对象不是本次对话,而是**会被下次装进窗口的持久物**。任务收尾时执行:逐面审计→按病理定性→输出清理清单→确认后执行→按标准验收。

## 一、审计面(七件套:污染住在哪)

逐件检查本次任务读写过的对应物:
1. **指令件**:系统提示、CLAUDE.md、技能/风格包、规则文件
2. **输入件**:用户的裁定与参照(本次新增了哪些,记录了吗)
3. **检索件**:记忆召回、RAG/材料文档、被引用的旧文件
4. **工具件**:工具说明、脚本用法注释
5. **短期笔记**:会话摘要、压缩产物、交接说明
6. **长期记忆**:memory 文件及索引
7. **输出格式件**:模板、产出约定

## 二、病理(四种失败模式:污染长什么样)

- **中毒**:凭摘要/凭记忆写下的断言进了持久层,被后续当真。测试:这条断言写下前,重读过原文吗?没重读的一律标"待验证+日期"、当场核实或删除;可机器验证的(文本一致性、数值)加自动比对闸。
- **分心**:规则和历史越积越厚,压过了对任务本身的新判断。症状:产出像在逐条满足清单;改动只敢整体重写不敢精准修改。动作:把散落规则蒸馏成一条优先级链或一个判别测试,让规则数下降。
- **混淆**:没错但与本任务无关的上下文在场,把结果带偏(过时范例、不适用的惯例、触发面过宽的记忆)。动作:给它写范围注记("X 不作为 Y 的参照,日期"),收窄其触发描述。
- **冲突**:两个权威说法不一,默认信了旧的/近的。动作:在源头文件写明优先级链(如:用户当次指示 > 用户提供的参照 > 项目规范 > 通用规则),并把败方条目改掉或标"已作废,被 X 取代"——修源头,不立注意事项。

## 三、处方(四步法:怎么清)

- **写出去**:结论、裁定、进度写进窗口外的持久物,带日期与适用范围;窗口内不留孤本。
- **挑选**:持久物按需取用——收窄记忆/技能的触发面,别让每次会话默认满载。
- **压缩**:同一文件追加超过两层"追加/补充"时合并重写:当前有效裁定置顶,历史压成一行;定期跑记忆整理。
- **隔离**:大活按工序拆会话(材料/写作/生产/审查),各带最小上下文,只回传短结论,防一处噪音毒害全局。

## 四、验收(四标准:清完什么样算干净)

对每件清理后的持久物问:**相关**(每条都为未来任务服务吗)/**结构**(裁定与历史分得清吗)/**时机**(是按需加载还是常驻)/**压缩**(还能更短而不失真吗)。四问皆过才算清完。

产出:清理清单(问题→病理→动作→改哪个文件),执行后汇报改了什么、什么标了待验证。
